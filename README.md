# Minimal-PocketFlow-Chatbot
A clean, lightweight terminal-based AI chatbot built in Google Colab using a **minimal PocketFlow-style architecture** and the **TypeGPT API**.  This project demonstrates how to structure an LLM chatbot using small, composable nodes instead of monolithic scripts.
